{
  "metadata": {
    "name": "{{ item.version | lower}}",
    "displayName": "API {{ item.name | lower}} {{ item.version | lower}}",
    "description": "Generated by CMP"
  },
  "desiredState": {
    "specs": {
      "REST": {
        "openapi": "3.0.0",
        "info": {
          "version": "{{ item.version | lower}}",
          "title": "API {{ item.name | lower}}"
        },
        "paths": {}
      }
    }
  }
}
